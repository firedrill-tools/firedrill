# Third-party terms

`@firedrill/agent` is an Apache-2.0 wrapper around Anthropic's `@anthropic-ai/claude-agent-sdk` package. The Claude Agent SDK and its platform binaries are not relicensed by Firedrill; their use is subject to Anthropic's applicable legal agreements. Review the license and data-usage links shipped with that dependency before enabling the optional Agent.

The rest of the Firedrill local framework does not depend on the Claude Agent SDK and remains usable without installing `@firedrill/agent` or contacting Anthropic.
